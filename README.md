# Harness Core

Harness Core records agent runs as append-only JSONL traces and derives run summaries from those traces.

This milestone intentionally includes no model calls, orchestration loop, API, database, or UI. The package owns the trace contract only.
